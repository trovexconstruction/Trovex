const header=document.querySelector('.header');const progress=document.querySelector('.progress');const menu=document.querySelector('.menu');const links=document.querySelector('.links');const lang=document.querySelector('.lang');let current='ar';
function onScroll(){header.classList.toggle('scrolled',scrollY>20);const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h?scrollY/h*100:0)+'%'}addEventListener('scroll',onScroll,{passive:true});onScroll();
menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('.links a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));
lang.addEventListener('click',()=>{current=current==='ar'?'en':'ar';document.documentElement.lang=current;document.documentElement.dir=current==='ar'?'rtl':'ltr';lang.textContent=current==='ar'?'EN':'AR';document.querySelectorAll('[data-ar][data-en]').forEach(el=>{const v=el.dataset[current];if(el.tagName==='INPUT'||el.tagName==='TEXTAREA')el.placeholder=v;else el.innerHTML=v})});
document.getElementById('year').textContent=new Date().getFullYear();
document.getElementById('quoteForm').addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('name').value.trim(),company=document.getElementById('company').value.trim(),phone=document.getElementById('phone').value.trim(),type=document.getElementById('type').value,details=document.getElementById('details').value.trim();const msg=`طلب عرض سعر جديد - TROVEX%0A%0Aالاسم: ${encodeURIComponent(name)}%0Aالشركة/الجهة: ${encodeURIComponent(company||'-')}%0Aالهاتف: ${encodeURIComponent(phone)}%0Aنوع الطلب: ${encodeURIComponent(type)}%0Aالتفاصيل: ${encodeURIComponent(details)}`;window.open(`https://wa.me/201008877499?text=${msg}`,'_blank','noopener')});
